# Canonical runtime gate checkpoint

The canonical runtime branch is now formatted with the deterministic runtime-gate repairs applied. The next verification boundary is Linux CI on this exact branch head, followed by Render staging verification against the same commit.

Required lifecycle remains:

real market -> strategy/signal -> canonical decision identity -> OMAR -> governance/admission -> adaptive sizing -> execution -> receipt -> physical settled ledger -> settled outcome -> OMAR learning gate -> exact action attribution -> policy update -> OOS evidence -> promotion -> future OMAR influence.
